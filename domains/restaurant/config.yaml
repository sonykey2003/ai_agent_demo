# Reused from the golden demo's "restaurant" domain (data/eval-facing).
# Knowledge lives in docs/qa.csv; datasets here are for Galileo eval and
# context-adherence / coherence baselining.

domain:
  name: "restaurant"
  description: "Restaurant assistant for online help and customer support"

galileo:
  project: "shawn-otel-demo"
  log_stream: "restaurant"

model:
  default_provider: "local"
  temperature: 0.1

rag:
  enabled: true
  knowledge_dir: "domains/restaurant/docs"
  chunk_size: 1000
  chunk_overlap: 200
  top_k: 3

vectorstore:
  embedding_model: "nomic-embed-text"
  collection: "ai_agent_demo_restaurant"

dataset:
  qa: "domains/restaurant/docs/qa.csv"
  csv: "domains/restaurant/dataset.csv"
  jsonl: "domains/restaurant/dataset.jsonl"
  input_column: "input"
  output_column: "output"
